[project]
name = "daz-forge"
version = "0.1.0"
description = "DAZ Smart Content analyzer, DIM packaging helper, and pose conversion tool"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.11",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
