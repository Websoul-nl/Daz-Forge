[project]
name = "daz-forge"
version = "0.1.0"
description = "Personal DAZ Smart Content analyzer and DIM packaging helper"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.11",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]